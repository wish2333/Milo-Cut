在执行了2.1.1-2之后，现在我已经能够看到AI结果、侧边栏、编辑行为和高亮状态的修改了

现在仍然存在几个重要问题

- 分割之后同步继承标记状态的情况仍然存在
- 所有AI处理结果也都同步继承标记状态，导致即便我只点击一个确认，也导致全部建议都通过并且消失在侧边栏中
- 侧边栏宽度有限，往往看不见完整的建议说明
- Timelineswitch地方重命名，得先点击输入框才能确认
- Analysis的结果中确认和忽略在侧边栏都没有表示，我都不知道我确认过或忽略过哪些内容（AI结果由于统一管理并且确认之后就消失无法确认状态，但是猜测也有一样的问题）

