# Record: Phase 3 macOS 冒烟通过（rc 门禁）

> 日期: 2026-08-31 · 分支: `dev-3.0.0` @ 34c7dc7 · 平台: macOS（WKWebView, Apple Silicon）
> 范围: Phase 3 重点回归（M8 组件拆分 / M9 层级契约）+ plan §5 标准清单

## 冒烟结论

**全绿，零问题**。用户逐项执行引导清单后确认通过：

### A. Phase 3 重点回归

| 项 | 结果 |
|---|---|
| M8-1 设置五 tab 走查（通用/AI 引擎/LLM/导出/快捷键） | ✅ 控件、保存/回填、Test Connection、提示词编辑与预设、ESC 关闭全部正常 |
| M8-2b ASR 单源实效（设置导出 tab ↔ 工作区转写弹层双向同步） | ✅ 改一处两处生效，弹层保存后自动关闭 |
| M8-2a 三个工具栏弹层（开合/外点关闭/保存自动关闭） | ✅ |
| M8-2c 全局键盘回归（文本框内 Delete/方向键不拦截、Esc/Enter/Delete 多选链、Cmd 系） | ✅ |
| M9-1 右键菜单互斥（行菜单 ↔ 波形块菜单互相关闭）、滚动关闭 | ✅ |
| M9 层级（toast 压顶、弹层无遮挡错位） | ✅ |
| M9-3 深浅色下浮层观感 | ✅ |

### B. 标准清单（plan §5）

1200 段 SRT 导入 / 播放与 hover 预览 / 波形滚动缩放 / 千段跳转定位 / 吸附拆分（toast 提示）与合并 / undo×5·redo / 静音检测 / 导出 MP4+SRT / 关开项目持久化 —— ✅ 全部通过。

## 遗留项（不阻塞 rc）

- Windows（WebView2）rc 冒烟待补测（用户裁决延续 beta.2 模式：macOS 先行放行，补测后回填）
- v-memo 命中 Vue DevTools 复核（两平台，随补测窗口）
- 1167 段单字编辑重渲染行数 DevTools 验证（同上）
