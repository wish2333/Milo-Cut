# v3.0.3 beta.1 交付记录（M1 列表副轨闭环）

> 日期：2026-09　分支：`dev-3.0.3`　范围：P1-1 + P1-2 + P1-3（详见各自 record）
> tag `v3.0.3-beta.1`：**已打**（2026-09 用户指示跳过冒烟，补打于 P1-3 合入点 M1 交付态；原流程为清单 → 签字 → tag）

## 交付概览（S1 主项全部落地）

- **M1-1 选择器**：`useListTrackSelector`（会话视图态，null = 主轨）+ Timeline 头部 segmented 切换 + 删轨/切时间线回退兜底；主轨分支引用恒等零 diff。
- **M1-2 副轨渲染**：同一行组件 variant 多态（text/start/end/时长 + 绑定标记）；空轨空态卡 + 「新建字幕」（播放时间锚点 2s cue，媒体上界 clamp）。
- **M1-3 编辑**：useTrackEdit 列表入口（text/time）——同内核同防抖 key 与波形侧合并；后端拒绝回滚 + toast 原文；时间本地预校验；flush-on-switch。
- **M1-4 行操作**：单击 seek（复用 handleListSeek）；播放跟随高亮；右键菜单定位/编辑/删除此条字幕（无确认框）；撤销捕获层谓词表 4 行落地（1-3 行真实链路 vitest，第 4 行为 3.0.2 既有 handler 接线）。

## 门禁终态（P1-3 合入时点）

| 项 | 状态 |
|---|---|
| `uv run pytest` | 716 passed |
| vitest | 725 passed + 1（唯一失败 = P0-1 已登记挂载墙钟环境例；新增用例累计 +55） |
| vue-tsc / vite build / eslint / ruff | 全绿 |
| 红线五文件 diff | 空（后端零改动达标） |

## ★ 待用户动作（阻塞 beta.1 tag）

双平台冒烟清单（合并 PLAN beta.1 ★ 节点）：

1. **列表副轨编辑全链路**（双语工程）：切换主/副轨 → 文本改（含 3 次快速输入防抖合并观察）→ 时间改（含越界/重叠拒绝 toast）→ 右键删除 → undo/redo 全程对照波形区三层一致性（列表 / 波形 lane / 绑定偏移）。
2. **空轨建段**：空副轨空态卡「新建字幕」→ 段即时出现。
3. **smooth A/B 手感样本**（R2.3 默认值裁决输入，P2-1 开发用）：多行模式播放跟随 + 手动滚动混合操作各 3 分钟——记录瞬时写入 vs 平滑动画的主观手感与有无空白/跳变。smooth 调度器将随后在 P2-1 交付（默认关），此项冒烟结果用于裁决默认值并回写 SPEC M2-1。
4. **kbd 角标**：P2-2 交付后随 beta.2 清单一并验收（本节点不阻塞）。

> **发布裁决（2026-09）**：用户指示跳过冒烟直接发布——上列清单未执行；`v3.0.3-beta.1` 已补打于 P1-3 合入点（M1 交付态）；smooth 默认值维持 `false` 为最终裁决（SPEC M2-1 已回写）。真机反馈若触发问题，按总记录 §5 遗留清单流程处理。
